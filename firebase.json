// Multiple sites configuration:
{
    "hosting": [
      {
        "target": "bhookiehome-345", // Make sure this matches the site ID in the Firebase Console
        "public": "public", // Or whatever your public directory is
        "ignore": [
          "firebase.json",
          "**/.*",
          "**/node_modules/**"
        ],
        "rewrites": [ ... ] // Optional rewrites
      }
    ]
  }
  